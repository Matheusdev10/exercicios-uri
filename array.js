// var input = require('fs').readFileSync('stdin', 'utf8')
// var lines = input.split('\n')

// var A = lines.shift()
// var X = A.split(' ')
// o split ele cria um array e separa os valores do texto ( o split faz isso pelo delimitador que é o espaço )
// console.log(X)

var nomes = [matheus, jéssica, joão, lucas, gabriela, fernando]
for (let index = 0; index > nomes.length - 1; index--) {
  const element = nomes[index]
}
